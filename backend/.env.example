# Server Configuration
PORT=4000
BASE_URL=http://localhost:4000

# Database Configuration
# Create a MongoDB cluster and paste your connection string here.
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/YourDatabaseName?retryWrites=true&w=majority

# Security
# Random string for signing JSON Web Tokens
JWT_SECRET=your_jwt_secret_key_here

# Brevo (Sendinblue) API Configuration
# Sign up at https://www.brevo.com/ to get your API Key.
# 1. Create an account.
# 2. Go to "SMTP & API" -> "Create a new API Key".
# 3. Create a sender in "Senders & IPs".
# 4. Paste the API Key and Sender Email below.
BREVO_API_KEY=xkeysib-your-long-api-key-here
SENDER_EMAIL=your-verified-sender@example.com

# Cloudinary Configuration (Optional - for image upload)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# PixelBin Configuration (Optional - for image processing)
PIXELBIN_API_SECRET=your_pixelbin_secret
